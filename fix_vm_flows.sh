sed -i '/private val _lotteryRecords = MutableStateFlow<List<LotteryRecord>>(/,/    val lotteryRecords: StateFlow<List<LotteryRecord>> = _lotteryRecords.asStateFlow()/c \    val lotteryRecords: StateFlow<List<LotteryRecord>> = _currentUserId.flatMapLatest { uid -> repository.getLotteries(uid) }\n        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())' app/src/main/java/com/example/ui/ExpenseViewModel.kt

sed -i '/private val _investments = MutableStateFlow<List<InvestmentItem>>(/,/    val investments: StateFlow<List<InvestmentItem>> = _investments.asStateFlow()/c \    val investments: StateFlow<List<InvestmentItem>> = _currentUserId.flatMapLatest { uid -> repository.getInvestments(uid) }\n        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())' app/src/main/java/com/example/ui/ExpenseViewModel.kt
